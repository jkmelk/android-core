package com.core.prefrences

enum class PreferenceKey(val keyName: String) {
    TOKEN("token"),
    REGISTRATION_KEY("Bearer "),
    DISPLAY_MODE("display mode"),
    VERSION_CODE("version code"),
    VERSION_NAME("version name"),
    APPLICATION_ID("Aplication id"),
}